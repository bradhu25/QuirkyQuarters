import 'package:flutter/material.dart';


class ReceiptSummaryRoute extends StatelessWidget {
  const ReceiptSummaryRoute({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Receipt Summary'),
      ),
      body: Center(
        
      ),
    );
  }
}